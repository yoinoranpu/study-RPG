# 開発ログ

このファイルは開発の現状を把握するためのログです。**Claude(このプロジェクトで作業するAI)は、作業内容が一区切りつくたびにこのファイルを更新すること。** 特に「進行中」セクションは常に最新の状態に保つ。過去のセッションが長くなりコンテキストが尽きても、このファイルを読めばすぐ状況を把握できるようにする。

## 進行中(今ここ)

**鍛冶屋の強化タブ: 強化アクションを吹き出しに移動してスペース削減**

- 「強化タブの強化を吹き出しのとこでできるようにしてすぺーすさくげんしてみやすいUIにしよう」との依頼。合成/スキル書タブは既に吹き出しでアクションが完結する作りだったが、強化タブだけは吹き出しが情報表示のみで、実際の強化ボタン(素材ごとに最大3-4行、秘石ボーナス込みで最大150px超)がリスト側に残っていた
- 吹き出し内、アイテム見出しの下に「素材オプションのチップ」を横並びで追加。各チップは効果ラベル(ATK強化など)+素材名×所持数を表示する小さいボタンで、タップで直接`upgrade(mo)`を実行(素材か所持Gが足りなければグレーアウトして押せない)。秘石(`forgeStone`)を所持している場合は「💠秘石で+2(無料)」チップも同じ並びに追加、`useForgeStone()`を直接実行
- リスト側の詳細カードからは素材選択行・秘石行を丸ごと削除し、レアリティ/現在ステータス/固有能力/ランダム能力/節目ボーナスの表示専用に整理。見出し文言も「（詳細は上の吹き出しに表示されます）」→「（強化は上の吹き出しから行えます）」に変更
- `upgrade()`/`useForgeStone()`関数自体は変更しておらず、呼び出し元(ボタンの設置場所)を移しただけなのでロジック面のリグレッションリスクは低い
- 実機で確認: アイテム選択後、吹き出しに素材チップが表示され所持数0の状態では正しくグレーアウトされること、リスト側が大幅に短くなったことを確認。素材を実際に消費しての強化実行は、開発環境のセーブデータがFirebase同期で頻繁に上書きされるため(既知の制約、materialsへのlocalStorage注入がリロード後に消えてしまう)目視確認できなかったが、ボタンのdisabled判定・onClickの配線は合成タブと同一パターンのため大きな懸念はない。コンソールエラーなし

**フィードバック反映その2: ギルドの記録パネルを元の位置に戻し、強化タブも合成と統一**

- 「ギルド画面記録とかは今までどおりの場所のほうがいい」「強化も合成と同じようにかえよう」との依頼
- **ギルド画面**: 直前のコミットで固定エリアに移動したLv/学習時間パネルを、元通り詰め所シーンの上・スクロール領域内の位置に戻した。`git diff`でd48ac2f(このセッションでギルド画面を触る前の状態)とTownPage.jsxが完全に一致することを確認済み。これによりスクロール不要は達成できなくなるが、ユーザーの優先度は「記録の表示位置」の方が高いと判断
- **鍛冶屋の強化タブ**: 吹き出しのアイテムアイコンをタップして選択解除できるよう、合成/スキル書タブと共通の`SynthBox`部品を使うよう変更(手書きだったアイコン表示コードを`SynthBox`呼び出しに置き換え、×バッジ付きで解除可能に)。ついでに`SynthBox`/`SynthPreview`に`tint`(色変えアイテム用のCSSフィルター)対応を追加(既存のItemPickerでは対応済みだったが、吹き出しプレビューでは未対応だった漏れを合わせて修正)
- 実機で確認: ギルド画面のレイアウトが完全に元通りになったこと、強化タブでアイテム選択後に吹き出しの×で解除できることを確認。コンソールエラーなし

**フィードバック反映: ギルド画面の縮小をやめて別方式に、合成UXを1リスト2タップ方式に作り直し**

- 「ギルドは画像が小さくなっちゃっておかしい」「合成はアイテムの表示を変える必要はなくて…1つ目にクリックしたものが合成元、2つ目に選択したものが素材、吹き出しのとこにセットしたアイテムをクリックしたら選択が解除される」との差し戻し。直前のコミットで入れた2つの対応が両方とも設計として不採用になったため作り直した
- **ギルド画面**: 前回は詰め所シーン(`guild_wall.png`)自体に`maxWidth:560`を付けて縮小・中央寄せしていたが、上の統計パネルは横幅いっぱいのままだったため「シーンだけ縮んで浮いて見える」不整合が発生していた。根本的に見直し、詰め所シーンの幅は完全に元通り(横幅いっぱい)に戻した上で、シーンの上に表示していたLv/G/学習時間の`.rpg-panel`カードを丸ごとスクロール領域の外(タブバー直下の固定エリア)に移動。Lv/Gは元々ヘッダーに重複表示されていたためそちらは削除せず残し、新しい固定エリアには重複しない情報(今日/今週/累計の学習時間、通算深度)だけを1行のスリムな帯として表示。これによりスクロール領域が「バナー余白+詰め所シーンのみ」になり、詰め所シーンの見た目・サイズを一切変えずに1014px幅で overflow 183px→0px、375px幅でも0pxを確認
- **鍛冶屋の合成/スキル書タブ**: 前回の「ベース選択リスト⇔素材選択リストが同じ枠内で入れ替わる」2段階UIを廃止し、`ItemPicker`を1つだけ常時表示する方式に変更。1回目のタップで合成元(`sel`)、2回目のタップで素材(`matSel`)を選択(`pickSynth`/`pickBook`関数が判定: 既に選択済みのものを再タップで解除、ベース未選択なら合成元として選択、ベースと噛み合わない種類・レアリティのものをタップした場合は合成元の選び直し扱い)。`ItemPicker`は`matUid`propを追加して2つの選択状態を同じグリッド内で同時にハイライト表示できるように拡張(選択中セルの左上に①②の小バッジを表示して区別)。吹き出し内の合成プレビュー(`SynthPreview`/`SynthBox`)にも×ボタンを追加し、吹き出しのアイコンをタップすることでも選択解除できるように
- 実機で確認: 合成タブ(聖なるローブ2着を同一グリッド内で1回目→2回目とタップして選択、噛み合わない鉄の剣をタップするとベースの選び直しになることを確認、吹き出しの×ボタンでの解除も確認)・スキル書タブ(同様の動作を確認)・強化タブ(単一選択のトグル動作に影響がないことを確認)。コンソールエラーなし

**スキル書37冊に系統別アイコン(6枚)を割り当て**

- 「アイコンがないもの(元のアイコンのままのもの)に変わりのアイコンを用意する」との依頼。調査の結果、`items.js`(装備)・`chest_table.js`(宝箱)は全項目に`image`が用意済みだったが、`skills.js`の全37スキル書だけは絵文字`icon`のみで専用イラストが一枚もなかった
- 37冊ぶん個別にイラストを発注するのはコスト過大なため、`AskUserQuestion`で方針を確認 → ユーザーが「系統ごとに1枚(6枚)」を選択。系統は`tree`フィールドの6種(sword/magic/bow/thief/defense/explore)
- ユーザーが本のドット絵アイコン6枚(表紙の色と紋章だけ系統ごとに異なる、共通シルエット)を納品。`skillbook1〜6.png`を`skillbook_sword/magic/bow/thief/defense/explore.png`にリネームして配置
- `skills.js`: `SKILL_TREE_ICON`(tree→画像パスのマップ)を追加し、`SKILL_BOOKS`の全エントリに`b.tree`に応じた`image`フィールドをループで一括付与(37エントリを個別に書き換える必要なし)
- 画像を表示する4箇所を修正: `ForgeTab.jsx`の`ItemPicker`(`getImage`propを新設、既存の`it.image`直接参照だけだと本の個体オブジェクトには`image`がないため)・`SynthPreview`/`SynthBox`(吹き出し内のプレビューにも`getImage`を追加、装備側もついでに対応させ表示を統一)・`CharacterPage.jsx`の`BookRow`(書一覧行)と`BookSlotCell`(装備スロット)・`SkillBookTab.jsx`の図鑑行。いずれも`image`があれば`<img>`、なければ従来の絵文字`icon`にフォールバックする作り
- 実機で確認: 鍛冶屋の合成/スキル書タブ(一覧・吹き出しプレビューとも系統色のイラストで表示、剣術=赤い本、魔法=紺の本など系統ごとに区別できる)、キャラ画面のスキルタブ(スロットにセットした本もイラスト表示)、図鑑のスキル書タブ(発見済みの本がイラストで表示、未発見の❓は影響なし)を確認。コンソールエラーなし

**ギルド画面: 広い画面でスクロールが発生する問題を修正**

- 「ギルド画面でスクロールしなくていいよう画面を節約する」との依頼
- 原因調査: `TownPage.jsx`のギルドホーム画面で、詰め所シーン(`guild_wall.png`、掲示板・クエスト・図鑑・実績を配置)の幅が`width:100%`(コンテンツ幅いっぱい)で、`aspectRatio:1408/768`により高さが決まる作り。モバイル幅(375px)では問題なく収まるが、デスクトップ/タブレット幅(1014px)ではシーンだけで高さ553pxにもなり、ヘッダー・タブバー・ステータスパネル分と合わせると画面に収まらずスクロールが必須になっていた(実測: スクロール領域の`scrollHeight`942px vs `clientHeight`759px、183pxはみ出し)
- 詰め所シーンのコンテナに`maxWidth:560, margin:"0 auto"`を追加し、広い画面でも横幅いっぱいに引き伸ばされないよう上限を設けて中央寄せ。これにより高さも比例して縮み、はみ出しが解消(デスクトップ1014px幅で確認: はみ出し183px→0px)。モバイル幅(375px、元々560pxを下回るので影響なし)でも引き続きはみ出しゼロを確認
- 副次効果として、掲示板下の「図鑑」「実績」ラベルが以前は画面外に切れて見えなくなっていたが、これも修正後は表示されるようになった

**鍛冶屋: 合成/スキル書タブの素材選択を「ベース→素材」の2段階1リスト方式に変更**

- 「合成で素材を選ぶの下まで行かなじゃいけないの大変」との指摘。ドラッグ&ドロップ案も出たが、既存の一覧タップ方式を活かしつつ手間を減らせる「ベースを選ぶ→同じ枠に素材候補だけが並び替わって表示される」の2段階1リスト方式を採用(ユーザー選択: 個別ドラッグ&ドロップより実装・operabilityともにシンプル)
- `ForgeTab.jsx`の合成タブ・スキル書タブを、`baseItem`/`bookItem`が未選択なら「①ベース装備を選択」の一覧(`ItemPicker`)、選択済みなら同じ枠が「②素材装備をタップ」の一覧(`synthCandidates`/`bookCandidates`でベースと同種・同形状・同レアリティのものだけに絞り込み済み)に差し替わる作り。素材一覧の右上に「ベースを選び直す」ボタンを追加し、`baseItem`/`bookItem`と選択状態をリセットして①に戻せるように
- 実機で合成タブ(幸運の短剣→②素材なしメッセージを確認、聖なるローブ×2→②で候補1件に絞り込まれ選択後に吹き出しへ合成プレビューが正しく表示されることを確認)・スキル書タブ(斬撃の書→②で同種のみに絞り込まれることを確認)ともに動作確認。「ベースを選び直す」ボタンで①に戻ることも確認。コンソールエラーなし

**鍛冶屋: 合成/強化のプレビューを店主の吹き出しに移動して画面を節約**

- 「合成結果とか強化結果を示す□＋□＝□とかは店主の横の吹き出しに入れることによって画面の節約をしよう」との依頼
- `SynthPreview`(合成タブ・スキル書タブで使う⬜+⬜⇒⬜のプレビュー)を、リスト上部に大きな`.rpg-panel`カードとして常駐させる方式から、店主の吹き出し内にコンパクト表示する方式に変更(アイコン52px→44px、外枠の`.rpg-panel`は吹き出し自体が兼ねるので撤去、合成ボタンも吹き出し内に統合)。ベース未選択時は吹き出しに従来の案内文が出るだけなので、選択するまでは見た目も変わらない
- 強化タブも同様に、アイテムアイコン+名前+Lv+コストの見出し部分を吹き出しに移動。リスト側のカードはレアリティ表示+ステータス+固有能力+節目ボーナス+強化素材選択のみに整理し、その分縦に短くなった
- 吹き出しの表示はタブと選択状態を見て自動切替(強化=アイテム見出し、合成/スキル書=ベース選択時のみプレビュー、それ以外は従来の案内セリフ)
- 実機で3タブとも表示崩れなし、コンソールエラーなしを確認。実際のアイテムを選択した状態でのプレビュー表示は、開発環境のセーブデータがFirebase同期で頻繁にリセットされるため目視確認できていないが、既存の動作実績あるロジックをそのまま吹き出し内に持ってきただけなので大きな問題はないはず

**バグ修正: 図鑑アイコンで顔が見えない(情報は明るくなるが絵が真っ黒)**

- 「情報は明るくなってるけど顔は見れない」との報告。原因は`MonsterBookTab.jsx`の`MonsterRow`で、`item_slot_frame.png`(枠画像)にだけ誤って`zIndex:1`を付けていたこと。ポートレート(顔クロップ)を表示する内側のdivには`z-index`を指定していなかったため、CSSのスタッキング規則上`z-index:1`の枠画像が常に上に来てしまい、枠の暗い凹み部分がポートレートを覆い隠していた(未発見時の❓アイコンも同様に隠れて見えなくなっていた)
- アイテムボックスや装備欄など他の`item_slot_frame.png`利用箇所はどこも`z-index`を指定しておらず、DOM順序(枠→中身)だけで正しく重なっていた。`MonsterRow`だけこの`zIndex:1`が紛れ込んでいたのが原因なので、単純に削除して他箇所と統一
- 実機でオオカミの図鑑アイコンに顔が表示され、未発見モンスターの❓アイコンも見えるようになったことを確認。コンソールエラーなし

**図鑑: 敗北しても一度戦えばモンスターが判明するように変更**

- 「顔は倒せなくても１回戦ったことあれば見れていいと思う」との指摘。従来は`DungeonPage.jsx`の戦闘結果処理で、`monsterBook`(図鑑データ)の更新が`if (result.won)`ブロックの中にしかなく、戦闘に負けた場合はそのモンスターの名前・種族・アイコンが一切図鑑に記録されなかった
- 図鑑登録(名前・種族・アイコンが見える状態にする)を勝敗に関わらず戦闘終了時に必ず行うよう変更。討伐数カウント(`entry.count`、「×N」表示)は従来通り勝利時のみ加算されるロジックのまま維持(遭遇しただけでは0のまま)
- `MonsterBookTab.jsx`の「発見済み」判定も`entry?.count > 0`(討伐数で判定)から`!!entry`(遭遇済みかどうかで判定)に変更
- 実機でオオカミを撃破 → 図鑑で名前・種族・ステータス・討伐数×1が正しく表示されることを確認(勝利パスのリグレッションなし)。敗北パスは同じ登録処理をif文の外に出しただけなのでロジック的に問題ないはずだが、意図的に負けさせるのが難しく実機での直接確認はできていない。コンソールエラーなし

**図鑑モンスターの顔クロップ、全30体の最終調整値を反映**

- ユーザーが`MonsterPortraitEditor`で全30体を目視調整した最終JSONを`monsterPortraits.js`に反映(前回コミットで暫定値のままだった重装イノシシ・ゴブリンシャーマン・食人植物の3体も、バグ修正後の正しい画像を見ながら調整し直した値に更新)
- ビルド成功、これで図鑑のモンスター一覧アイコンは全て意図した顔クロップで表示される

**バグ修正: 図鑑の顔クロップで3体が正しく表示されない問題**

- 「重装猪が酸スライムになってた」「ゴブリンシャーマンが顔じゃなくて体になってる」「食虫植物の画像が設定されてない」の3件を調査
- **重装イノシシ(最重要バグ、根本原因を特定するのにかなり手間取った)**: 元画像`イノシシ(星2)胴体.png`のファイル名に**括弧**が含まれており、`backgroundImage: \`url(${src})\`` のように引用符なしでurl()を組み立てると、括弧を含むパスはCSSとして無効になりブラウザに黙って無視される。無視された結果、DOM上は「直前に表示されていた別モンスターの画像」がそのまま残ってしまい、まるで違うモンスターの絵に差し替わったように見えていた。`url("${src}")`と引用符で囲むよう修正(`MonsterPortrait.jsx`・ついでに同じ書き方をしていた`DungeonCanvas.jsx`も予防的に修正)。調査には`console.log`とDOMの`getComputedStyle`を直接比較する地道な切り分けが必要だった
- **ゴブリンシャーマン**: `GROUND_RIG_MONSTERS`の`body`画像が胴体のみ(頭は完全に別パーツ)という仕様のため、`body`をそのまま顔クロップに使うと顔が写らなかった。`getMonsterPortraitSrc`を、`parts`配列に`key:"head"`があればそちらを優先するよう修正(汎用的な条件分岐なので、今後同様の構成のモンスターが増えても自動的に対応できる)
- **食人植物(食虫植物)**: `multipartMonsters.js`側の参照が`食人植物.png`になっていたが、実際に存在するファイルは`食虫植物.png`(タイポ)。ファイル参照を修正(この画像は実際のバトル演出でも使われる`SWAY_IMAGE_MONSTERS`のデータなので、図鑑だけでなくダンジョン内の実際のモンスター表示のバグも同時に直った)
- 修正の影響で意味が変わった暫定クロップ値(重装イノシシ・ゴブリンシャーマン)は実機で見ながら調整し直した
- 実機で3体とも正常表示を確認。コンソールエラーなし

**バグ修正: 顔クロップエディタで縦位置が動かない問題 → ドラッグ+ホイール操作に刷新**

- 「縦に行かない　カーソルで合わせるのできたら一瞬でできそう」との報告
- 原因: `object-fit:cover` + `object-position` + `transform:scale`という組み合わせで実装していたが、`transform:scale`は`object-position`が適用された後に要素の中心を基準にかかるため、object-positionでの微調整とscaleでのズームが干渉し合い、特に縦方向がほとんど動いて見えない状態になっていた
- `src/components/MonsterPortrait.jsx`(新規)に`useMonsterPortraitStyle(id, crop, boxSize)`フックを作成し、画像の実寸(naturalWidth/naturalHeight)を取得した上で「cover相当のスケール×zoom倍率」からbackground-size/positionをpx単位で直接計算する方式に全面変更(transform:scaleを使わない)。これにより縦横とも正確に反応するようになった
- `MonsterPortraitEditor.jsx`を全面刷新: 数値ステッパーによる操作から、プレビュー画像を直接ドラッグ(位置)・ホイール回転(拡大率、range inputでも可)で調整できる方式に変更。ドラッグ量をpxから%へ変換する計算式は「minX/minY(パン可能範囲)に対する移動量の割合」を使用
- `MonsterBookTab.jsx`の一覧アイコンも同じ`useMonsterPortraitStyle`を使うよう統一(行ごとに`MonsterRow`サブコンポーネント化してフックのルール違反を回避)
- 実機でスライム(中央の目に正確に合わせられた)・ゴブリン(既定値のままでも顔がしっかり収まるようになった)を確認。フレッシュタブでコンソールエラーなし(dev serverの依存キャッシュが古い状態を掴んでいた形跡があったため、確認の過程でdev serverを再起動している)

**図鑑のモンスターアイコンに顔クロップ表示を追加**

- 「図鑑のモンスターのアイコンに顔部分を切り取って入れられないか」との依頼。新規の顔専用イラストは発注せず、既存の立ち絵アセット(`MULTIPART_MONSTERS`等の`body`/`src`、30体全てに用意済み)を`object-fit:cover`+`object-position`+`transform:scale`で拡大トリミングして使い回す方式で対応
- `src/data/monsterPortraits.js`(新規): `MULTIPART_MONSTERS`/`SIMPLE_IMAGE_MONSTERS`/`GROUP_IMAGE_MONSTERS`/`SWAY_IMAGE_MONSTERS`/`FLOAT_IMAGE_MONSTERS`/`FLOAT_RIG_MONSTERS`/`GROUND_RIG_MONSTERS`からモンスターIDごとの代表画像(+竜の色替えに使うtintフィルター)を解決する`getMonsterPortraitSrc()`と、クロップ位置(`x`/`y`=object-position%、`zoom`=拡大率)の既定値`MONSTER_PORTRAIT_DEFAULT`(全モンスター共通の暫定値からスタート)を追加
- モンスターごとに立ち絵の構図がバラバラ(直立の人型、4足獣、浮遊系、スライム等)で、共通の初期値だけでは顔が正しく収まらない個体が多い。RigEditor/FloatRigEditor/CharacterLayoutEditorと同じ「暫定値+DEBUG専用の自己調整ツール」方針を踏襲し、`MonsterPortraitEditor.jsx`(新規)を作成: モンスター選択+x/y/zoomのステッパー+ライブプレビュー+「全モンスター分JSONをコピー」ボタンで、目視調整した結果をそのまま`MONSTER_PORTRAIT_DEFAULT`に貼り戻せる
- `MonsterBookTab.jsx`にDEBUG専用ボタンで結線。図鑑のアイコンサイズも36→52pxに拡大し顔が見やすいように
- 実機で図鑑画面・エディタ双方を確認(モンスター切り替え・ステッパー操作でプレビューが正しく更新される)。フレッシュタブでコンソールエラーなし(旧タブには編集途中でファイル未作成だった一瞬のHMRエラーが残留していたが、これは既知のスケールHMRアーティファクトで実害なし)
- 各モンスターの最終的なクロップ値は実際の顔位置を見ながらの微調整が必要なため未完了。ユーザー側でDEBUGボタンから調整してJSONを貼り戻すか、次回の作業で私が一体ずつ確認しながら追い込む想定

**図鑑・クエスト・実績のリスト行を刷新**

- 「図鑑　クエスト　実績　も改良しよう」との依頼。いずれも見出しバナー(イラスト付き)は既に用意されていたが、中身のリスト行(モンスター/スキル書/クエスト/実績)は初期の「フラットな箱+色枠」のままだった
- `MonsterBookTab.jsx`/`SkillBookTab.jsx`: 各行のアイコン(36px、色付き四角+絵文字)を`item_slot_frame.png`枠+発見済みなら種族/レアリティ色のグローに変更(未発見はグレースケールの❓のみ)。行自体の装飾的な外枠も撤去し、レアリティ色の左帯(機能的)+暖色系の背景塗りだけに軽量化(BookRowと同じ考え方)
- `RewardRow.jsx`(クエスト・実績で共用): 同様に外枠を撤去し左帯のみに。`AchievementTab.jsx`の未解禁(🔒)行も同じ処理で統一
- `SkillBookTab.jsx`のヘッダー(唯一バナー画像がなく無地だった箇所)は新規イラスト発注せず、グラデーション+ドットフォント見出しで軽く底上げ
- 実機でクエスト・実績・図鑑(モンスター/スキル書とも)を確認、コンソールエラーなし

**バグ修正: ダンジョン背景のスクロール方向が逆だった**

- 「ダンジョンの背景が逆に動いてる」との報告。原因は、セッション中盤に行った背景スクロールのcanvas→CSSアニメーション化(`DungeonCanvas.jsx`のリライト)で、`@keyframes dungeonCorridorScroll`の`background-position-x`の符号を誤って逆にしてしまっていたこと
- 旧procedural版(`draw()`関数、offsetを減算していく实装)と見比べて方向を検証: 旧版は特徴(レンガ・松明等)が左→右に流れる実装だったのに対し、CSS版は`0 → -shift`という向きで右→左に流れていた。合わせ鏡タイルは画像の性質上どちら向きに流しても継ぎ目は破綻しない(接合点が2箇所とも鏡対称で一致するため)ため、これまで見た目に破綻は出ていなかったが、方向自体が意図と逆になっていた
- `to { background-position-x: calc(-1 * var(--corridor-shift)); }` → `to { background-position-x: var(--corridor-shift); }` に修正し、旧版と同じ左→右の流れに統一
- 実機でダンジョン1に入りSTARTを押して確認、壁の松明・蜘蛛の巣などの特徴が正しい向き(左→右)に流れることを確認。コンソールエラーなし

**ダンジョン内の戦闘UI・イベントポップアップを改良**

- 「ダンジョン内の改良」の第一弾として、ユーザーが選んだ「戦闘UI(HP/敵の表示)」「イベント(罠/妖精/NPC等)のポップアップ」に着手
- **HPバー(3箇所)**: `PlayerSprite.jsx`(通常表示・戦闘表示の2関数)と`MonsterSprite.jsx`(モンスター上部のHPバー)はいずれもcanvas描画のため、`item_slot_frame.png`のようなイラスト枠は直接使えない。代わりにcanvas描画のまま質感を強化: バーを太く(18→20px)、縁を無地の灰色からゲーム全体で使っているゴールドアクセント色(`rgba(201,150,61,0.85)`)に変更、塗り部分に`shadowBlur`でレアリティグローと同系統の光彩を追加、上部に半透明の光沢ハイライト帯を追加。新規アセット不要
- **画面上部HUDのHPバー**(常時表示): 幅40〜50px→60〜84pxに拡大、ゴールドの縁+innerシャドウを追加、表示を`hp`のみ→`hp/maxHp`に変更して情報量を増やした
- **イベントポップアップ**(罠・妖精・NPC・宝箱等の発生時表示): 無地の黒背景+色付き細枠だったのを、ショップ/鍛冶屋の吹き出しと同じ`.rpg-panel`(木枠)に統一。アイコンも28px→48pxの`item_slot_frame.png`枠+イベント色のグローに拡大(画像がない場合は絵文字がそのまま枠の中央に収まる作り、ItemDetailFrameと同じフォールバック方式)
- **戦闘結果ポップアップ**(撃破/逃走+報酬表示): 同じく`.rpg-panel`に統一、文字サイズも一段階拡大
- 実機でダンジョン1に入り、DEBUGボタンでモンスター戦闘・宝箱イベントを複数回発生させて確認。モンスターHPバーのグロー、イベントポップアップの木枠+アイコン枠、いずれも正常表示。コンソールエラーなし

**鍛冶屋の名前検索欄を撤去、キャラのステータスラベルを日本語化**

- 「鍛冶の名前で検索いらないかもね」→ `ForgeTab.jsx`の`ItemPicker`から検索入力欄と`query`stateを削除。装備/スキル書の所持数はそこまで多くならない前提なので、検索なしでもレアリティ別グループ表示で十分閲覧できる
- 「キャラのステータス日本語のほうがわかりやすい」→ CharacterPageのステータスプレート表示をATK/MAG/DEF/MDEF/EVA/CRITから攻撃/魔力/防御/魔防/回避/会心に変更。内部的な`label`(英語)は`charLayout.statItemPos`のキーとして引き続き使用し、表示用に`jp`フィールドを追加する形で実装(レイアウトエディタや保存済み位置データへの影響なし)
- 実機で両方確認、コンソールエラーなし

**鍛冶屋の作業台背景イラスト納品・適用**

- `forge_workshop_bg.jpg`(木製ワークベンチ+背後に工具・鉱石・金床のシルエット、暖色の炉明かり、1408×768)を配置。既に仕込んでおいた参照先にファイルを置くだけで反映
- 実機確認: 強化タブの背後にくっきり表示、視認性も問題なし。コンソールエラーなし

**鍛冶屋もショップと同様に仕上げ**

- 「鍛冶屋も同様に仕上げよう」との依頼。ShopTabで行った一連の改修(木枠の吹き出し・イラスト枠のアイテムグリッド・背景)を`ForgeTab.jsx`にも展開
- 鍛冶屋の吹き出しを`.rpg-panel`に統一(Shopと同じ、三角の吹き出し先も同様に撤去)。未使用になった`BUBBLE_BG`/`BUBBLE_BORDER`定数を削除
- 強化/合成/スキル書の3タブすべてで使われる共通コンポーネント`ItemPicker`のグリッドタイルを、フラットなレアリティ色ボックス→`item_slot_frame.png`+レアリティグロー+`.slot-cell`/`.slot-selected`に統一(1箇所の修正で3タブ全部に反映)
- 合成プレビューの`SynthBox`(⬜+⬜⇒⬜)も同じ枠画像を流用した小型版に変更。`SynthPreview`と強化タブの装備サマリーカードも`.rpg-panel`でラップし、装備サマリーのアイコンも枠付きに拡大
- 所持素材ボックスは重い枠を外し、上に薄い罫線を引くだけの軽量表示に変更(スキルタブのセクション区切りと同じ考え方)
- 商品グリッド欄と同じ要領で、作業台エリアに`forge_workshop_bg.jpg`(未納品)を参照する形で仕込み済み
- 実機で強化/合成/スキル書の3タブとも確認。ローカルセーブが空(Firebase同期でリセットされる開発環境の既知の挙動)のためアイテムありの状態のグリッド表示は目視未確認だが、アイテムボックス/ショップで動作実績のある同一ロジックの流用。コンソールエラーなし

**店主の吹き出しを木枠デザインに刷新**

- 「吹き出しもそれっぽいRPGっぽいのを用意しよう」との依頼。従来は無地の暗い角丸+黄色っぽい細枠+三角の吹き出し先という、いかにもCSSで組んだだけの見た目だった
- 新規イラスト発注はせず、既存の`.rpg-panel`(木枠+リベット、`ui_frame_wood.png`使用、ギルド画面の情報カード等で実績あり)をそのまま適用して解決。三角の吹き出し先は、16px幅の木枠との継ぎ目が不自然になるため撤去(店主のすぐ隣に配置されているので無くても「誰が喋っているか」は明確)
- 未使用になった`BUBBLE_BG`/`BUBBLE_BORDER`定数も削除
- 実機確認: 木枠のダイアログボックスとして表示、コンソールエラーなし

**ショップ棚背景イラスト納品・適用**

- `shop_shelf_bg.jpg`(木製カウンター+背後の棚に瓶・巻物、低ディテール、1376×768)を配置。既に仕込んでおいた参照先にファイルを置くだけで反映
- 実機確認: 商品タイルの背後にくっきり表示、視認性も問題なし。コンソールエラーなし

**ショップ: 吹き出しの文字・アイコンを拡大、商品グリッドに背景を仕込み**

- 「吹き出しのアイテムアイコンが小さい、説明も字も大きく」との指摘。アイコンを36px→56pxに拡大、名前(12→15px)・レアリティ/SOLD(10→12px)・ステータスchip(10→12px)・天性/アビリティ(10→12px)・説明文(9→11px)・価格(13→15px)を一段階ずつ拡大
- 商品グリッド欄(③)に`shop_shelf_bg.jpg`(未納品)を参照する形で仕込み、届くまでは暗いグラデーションのみ(ダンジョン背景と同じ方式)
- 実機で武器タブを確認、吹き出しの可読性が改善。コンソールエラーなし

**ショップの改良: 商品グリッドをイラスト枠化、店主吹き出しを軽量化**

- 「次はショップの改良に取り掛かろう」との依頼。`ShopTab.jsx`の商品グリッドが、アイテムボックスを改良する前と同じ「レアリティ色のフラットな箱+色枠」のままだったのが最後の未着手箇所だった
- 商品タイルを`item_slot_frame.png`+レアリティ色グロー+`.slot-cell`/`.slot-selected`(選択中は拡大+金グロー)に統一。装備スロットと同じく「枠+ラベルは下」の構成に変更(以前は枠内に画像+名前+レアリティ+価格を全部詰め込んでいた)
- 店主の吹き出し内のアイテムアイコンも22px→36pxの枠付きアイコンに拡大。天性(◆)・アビリティ(✦)を囲っていた個別の色枠+背景ボックスは、吹き出し自体がすでに枠として機能しているため撤去し、テキストの余白のみに軽量化(前回のCharacterPageの「土台撤去」と同じ考え方)
- ついでに見つけた`borderLeft`+`border`ショートハンド衝突のバグ修正はShopTab.jsxの前回コミットで対応済み
- 実機で武器/特殊タブとも確認、選択中の商品が正しく拡大+グローする。コンソールエラーなし

**「タブの中のタブ」も全てドット絵アイコン化**

- ユーザー納品の11種のドット絵アイコン(装備/スキル/強化/合成/スキル書/武器/防具/アクセ/消耗品/特殊/モンスター)を、以下4箇所のサブタブに適用。いずれもメインタブと同じ`.slot-cell`/`.slot-selected`(拡大+金グロー)方式+`tab_bar_bg.jpg`背景で統一:
  - `CharacterPage.jsx`: 装備/スキル
  - `ForgeTab.jsx`: 強化/合成/スキル書
  - `ShopTab.jsx`: 武器/防具/アクセ/消耗品/特殊
  - `TownPage.jsx`図鑑: モンスター/スキル書(鍛冶屋と同じ`tab_icon_skillbook.png`を使い回し)
- 調査中、`ShopTab.jsx`の初回限定サポートバナーで`borderLeft`+`border`ショートハンドが衝突し左帯が正しく効いていなかった既存バグ(BookRowで直したのと同じパターン)を発見、あわせて修正
- 実機で4箇所とも確認(モンスター/スキル書/装備/スキル/武器〜特殊/強化〜スキル書)。フレッシュタブでコンソールエラーなし(古いHMRタブでは無関係な過去のborder警告が残留していたが、フレッシュタブでは再現せず=既知のスケールHMRアーティファクト)

**タブバー背景イラスト納品・適用**

- `tab_bar_bg.jpg`(革ベルト風の細いストライプ、ステッチ入り、2928×352)を`.jfif`→`.jpg`変換の上、既に仕込んでおいた参照先に配置。コード変更不要で反映
- 実機確認: 4アイコンとも背景の上でくっきり視認できる。コンソールエラーなし

**メインタブ(ギルド/キャラ/ショップ/鍛冶屋)をドット絵アイコン化**

- 画面上部のメインナビゲーション4タブの絵文字(🏰🧙🏪🔨)を、ユーザー納品のドット絵アイコン(`tab_icon_guild.png`盾+塔クレスト、`tab_icon_character.png`角付き兜、`tab_icon_shop.png`金貨袋、`tab_icon_forge.png`金床+ハンマー)に差し替え
- 選択中タブの表現も、アイテム/装備/スキルスロットと同じ`.slot-cell`/`.slot-selected`(拡大+金グロー)方式に統一。非選択タブは`opacity:0.65`で少し控えめに
- タブバー自体の背景に`tab_bar_bg.jpg`(未納品、シンプルな板/革のストライプを想定)を参照する形で仕込み、届くまでは暗めのグラデーションのみ表示(ダンジョン背景と同じ「先にコードだけ仕込む」方式)
- 納品時に余分な2枚(`レイヤー 3.png`/`レイヤー 5.png`)も一緒に置かれたが、ユーザーの指示により未使用のまま放置(削除もしていない)
- 実機で4タブすべて確認、選択中アイコンの拡大+グローも正常表示、コンソールエラーなし

**アイテムボックス/装備/スキルの選択中表示を統一・スキルスロットにも選択機能を追加**

- 「アイテムボックスとかスキルのところも、ギルドタブと同様に選択中がわかりやすいようにしたい」との依頼
- 原因: アイテムボックス/装備枠の「選択中」はレアリティグローの色を紫(`#a78bfa`)に差し替える方式だったが、epicレアリティの色も同じ`#a78bfa`のため、epicアイテムが非選択でも常に紫グローになり「選択中なのか単にepicなのか」が見分けづらかった
- `App.css`に`.slot-cell`/`.slot-selected`を新設。選択中は色に頼らず**拡大(scale 1.1)+金色グロー**(ギルド掲示板の紙をタップした時の押下演出と同系統の配色)で示す方式に統一。レアリティ/種類のグローは選択状態に関係なく常にアイテム本来の色のまま表示されるよう分離
- 適用箇所: アイテムボックスの各マス、装備4枠、特殊(消耗品)3枠、そして**新規にアクティブ/パッシブ書スロット(`BookSlotCell`)にもクリックで選択できる機能を追加**(今までは×ボタンでの削除とドラッグのみで、選択状態という概念自体がなかった)
- 実機確認: 装備/スキルタブともビルド成功・コンソールエラーなし。ただしこの時点でブラウザのローカルセーブがFirebase同期で空(アイテム0個)の状態だったため、選択時の拡大+金グロー表示そのものの目視確認は次回アイテムがある状態で要再確認(ロジックは同一パターンの使い回しで、アイテムボックスでは元々このメカニズム自体は動作実績あり)

**装備/スキルタブから最初期の「半透明黒+色枠」ボックスを撤去**

- 「最初に土台を作ったときの半透明の黒い視覚と色のついた枠をなくしたい。なくして問題ないところはなくして、おかしくなるところは代わりのイラストを用意する」との依頼
- 対象はCharacterPage.jsxの3箇所: 装備タブの装備枠+特殊枠を囲っていた`rgba(15,10,5,0.72)`+金枠のカード、スキルタブのアクティブ書/パッシブ書セクションを囲っていた同型カード。いずれも各要素(装備枠・特殊枠・アイテムスロット)がすでに`equip_slot_frame.png`/`item_slot_frame.png`のイラスト枠を持っているため、外側の箱はもう視認性のために必要なく撤去
- 撤去すると見出しテキストの区切りが弱くなる箇所(スキルタブの「アクティブ書」「パッシブ書」の境目)は、箱の代わりに見出し下の細い罫線(`borderBottom`)+`textShadow`+余白のみで区切りを表現。新規イラストを用意するまでもなく、実機確認でグループ分けは十分わかりやすい
- あわせてBookRow(所持スキル書一覧の行)の装飾的な外枠(`border:1px solid...`)も撤去し、機能的に意味のあるレアリティ色の左帯(`borderLeft`)とテキスト可読性のための背景塗りだけを残した(以前は`border`ショートハンドが`borderLeft`を上書きしてしまい3px帯が実際には効いていなかった副次的なバグも解消)
- 実機で装備タブ・スキルタブ双方を確認: 背景イラストの上に枠付き要素が直接乗る見た目になり、まとまりも保たれている。コンソールエラーなし

**スキル部屋の背景イラスト納品・適用**

- `skill_room_bg.jpg`(書斎/本棚、キャンドル、羊皮紙のドット絵)を`.jfif`→`.jpg`変換の上、コードに仕込んでおいた参照先にそのまま配置。追加のコード変更は不要(先にファイル名だけ仕込んでおいた形が想定通り機能した)
- この納品を機に、**今後の新規背景イラストのプロンプトには基本的に「ドット絵/pixel art」指定を入れる方針**に統一(既存のcharacter_room.jpg/guild_wall.png/item_box_bg.jpgなど塗り絵風の既存アセットはそのまま維持、遡って作り直しはしない)
- 実機で確認: アクティブ/パッシブ書カードの背後に書斎の背景が表示され、枠や配色との馴染みも良好。コンソールエラーなし

**スキル欄を装備欄と統一感のあるデザインに刷新**

- 「スキル欄も装備欄を参考に改良できる？」との依頼。旧デザインは無地の`#0d0d15`ボックス+フラットな色付き矩形スロットで、装備タブのイラスト化から取り残されていた
- アクティブ書/パッシブ書のセクションを装備タブと同じ`rgba(15,10,5,0.72)`+金枠のカードスタイルに統一。スロットは新規`BookSlotCell`コンポーネントとして切り出し、アイテムボックスと同じ`item_slot_frame.png`+レアリティ色グローを流用(空きスロットは"S1"/"P1"等の番号のみ表示、セット済みはアイコン+名前ラベル+×ボタンをフレーム上にオーバーレイ)
- 所持書一覧の行(`BookRow`)も暖色系の半透明カード(`rgba(15,10,5,0.6)`+金系の枠線)に統一し、アイコンを16→19pxに拡大
- タブ全体の背景に`skill_room_bg.jpg`(未納品)を参照する形で仕込み済み。まだファイルが無いのでCSSのグラデーション(暖色の暗い陰影)のみ表示される状態だが、画像を配置すれば自動的に反映される(ダンジョン背景と同じ「先にコードだけ仕込む」方式)
- 実機確認: 空きスロット状態(S1〜S4, P1〜P6)で新フレームが正しく表示、コンソールエラーなし。所持書がある状態(グロー色・BookRow配色)はローカルの一時テストデータがFirebase同期で上書きされ確認できなかったが、アイテムボックスで検証済みの同一ロジック(`item_slot_frame.png`+グロー)を流用しているため実装への信頼度は高い

**アイテムボックス背景イラスト適用**

- ユーザーが提案した3案(木の道具箱/石壁の武具ラック/布の敷物)から「木の道具箱」を採用。`.jfif`(実体はJPEG、いつものパターン)で納品されたので`item_box_bg.jpg`に変換して`public/assets/images/`に配置
- アイテムボックスの`flex:1, overflowY:"auto"`コンテナに`background: linear-gradient(暗いスクリム) + url(item_box_bg.jpg)`を設定。スクリムを挟むことで見出しテキスト("ITEM BOX x/30")の視認性を確保しつつ木目を活かした。グリッドがスクロールしても背景はコンテナに固定されたまま(コンテンツだけが上を流れる)ので違和感なし
- 実機で確認: 枠・レアリティグローとも背景の上で問題なく視認できる、コンソールエラーなし

**アイテム詳細パネルの統一・バグ修正(装備中スロットをタップすると古いパネルが出る)**

- 原因判明: アイテムボックスのアイテム詳細パネルだけを`.rpg-panel`+大きめアイコンに更新していたが、装備中スロット(`sel.startsWith("slot_")`)と消耗品スロット(`sel.startsWith("cslot_")`)用の詳細パネルは別ブロックとして残っており、更新前の無地の黒背景+24pxアイコンのままだった。装備中の武器/防具/アクセをタップすると必ずこの「古い」パネルが表示される状態だった
- `ItemDetailFrame`という共通コンポーネントを新設し、アイテムボックス/装備スロット/消耗品スロットの3箇所すべてで使い回すよう統一。アイコンを60pxに拡大(以前の36px/24pxより見やすく)、パネル全体に`maxHeight:"58%"`+内側`overflowY:"auto"`を設定し、アビリティが多いアイテムでもパネルが際限なく伸びて画面を圧迫しないようにした
- 実機で装備中武器/アイテムボックスのアイテム両方をタップして確認、同じデザインのパネルが表示されコンソールエラーなし

**アイテム詳細ポップアップのイラスト化・長押しドラッグ時のテキスト選択バグ修正**

- アイテムボックスのアイテムをタップした時に下からせり上がる詳細パネルを、無地の黒背景から`.rpg-panel`(既存の`ui_frame_wood.png`木枠、他のカードで使い回している9-slice border-image)に変更。アイコン部分も`item_slot_frame.png`+レアリティ色グローの小型版を新設し、装備枠/アイテムボックスと統一感を持たせた。新規アセットは追加していない(既存素材の再利用のみ)
- モバイルでアイテムを長押しドラッグすると、ブラウザ標準のテキスト選択(青いハイライト)や選択ハンドル・コールアウトメニューが出てしまう不具合を修正。`beginDrag`の起点になる要素(アイテムボックスのマス、スキルブックの行)に`userSelect:"none"`, `WebkitUserSelect:"none"`, `WebkitTouchCallout:"none"`を追加(`touchAction:"none"`だけではスクロールは止まるが選択UIは防げなかった)
- `characterLayout.js`: ユーザーがエディタで調整したJSON(`statItemPos`のDEF/MDEF: y60→40, EVA/CRIT: y120→80、密度を上げて部屋の高さ内に収まるように)を反映
- 実機(ローカルセーブに実在するアイテムで)確認: レアリティごとのグロー色、詳細パネルの木枠、アイコンの枠、いずれも正常表示。コンソールエラーなし

**アイテムボックスのイラスト化**

- キャラ画面「装備」タブ下部のアイテムボックス(6列×最大30マス)が無地の暗い枠(`#0a0a12`+破線)だったのを、装備枠と同じ「枠画像+背後にレアリティ色のグロー」方式に統一
- ユーザーが`item_slot_frame.png`(1022×1012、透過PNG、ブロンズ+ケルト結び目装飾の枠)を用意。**レアリティは枠自体の色を変えるのではなく**、`equip_slot_frame.png`と同じく枠は共通の中立デザインのまま、背後に敷く`blur(6px)`のレアリティ色グロー(選択中は`#a78bfa`)と、枠内下部のレアリティ色バーで表現する方式を踏襲(枠素材を1つに保てるメリットを維持)
- `CharacterPage.jsx`のItem Box部分を全面差し替え: 各アイテムセルは`position:relative`のコンテナに①グロー div → ②`item_slot_frame.png` → ③アイコン/画像 → ④+強化値・装備中バッジ・レアリティバー(いずれも`zIndex:1`で枠の上に表示)を重ねる構造。空きマスも同じ枠画像を`opacity:0.5`で表示(以前は破線のプレースホルダーのみだった)
- ブラウザで確認: 空きマス30個が枠画像で正しく表示されコンソールエラーなし。所持アイテムがある状態でのグロー表示は装備枠で確認済みの同一ロジックの流用のため未実機テストだが、ロジックは装備枠と同一
- **次の課題候補**: ショップ/鍛冶屋タブの背景・枠イラスト化(プロンプトは以前ドラフト済み、まだユーザーから素材未受領)

**ダンジョン背景のイラスト化(コード側は準備完了、イラスト待ち)**

- 現状のダンジョン背景は画像ではなく`DungeonCanvas.jsx`が毎フレーム描く完全プロシージャル(レンガ壁・松明・床タイル)。「背景が1パターンで単調」との指摘を受け、各ダンジョンの通路+ボス部屋のイラストに差し替えることにした
- ダンジョン1(スライム/骸骨/コボルト系): 湿った石造りの入門ダンジョン、ダンジョン2(ゴブリン/デスナイト系): 崩れた古城・遺跡、ダンジョン3(フェンリル/竜系): 溶岩の坑道・地獄めいた最終ダンジョン、という3段階の世界観で通路+ボス部屋を計6枚用意する方針
- **実装済み**: `DungeonCanvas.jsx`に`dungeonId`/`bossFloor` propを追加(`DungeonPage.jsx`から渡す)。以下のファイル名で画像を`public/assets/images/`に置くだけで自動的に反映される仕組み:
  - `bg_dungeon1_corridor.png` / `bg_dungeon1_boss.png`
  - `bg_dungeon2_corridor.png` / `bg_dungeon2_boss.png`
  - `bg_dungeon3_corridor.png` / `bg_dungeon3_boss.png`
  - 画像が無い間は自動的に既存のプロシージャル描画にフォールバックするので、今すぐ壊れることはない(`img.complete && img.naturalWidth`でチェック)
  - 通路は継ぎ目のないイラストを用意する必要がないよう、**合わせ鏡方式**(奇数タイルは反転して並べる)でループさせている。ボス部屋は一発勝負の見せ場なのでスクロールさせず静止画のままobject-fit:cover相当で表示
  - 松明の明滅・四隅の暗いフォグ(ビネット)は共通の前景演出として`drawTorches`/`drawFogVignette`に切り出し、プロシージャル版・イラスト版どちらでも同じものを重ねて使う
  - 画像サイズ目安: 通路は横長パノラマ(2000×800px程度)、ボス部屋は画面比率に近い静止シーン(1200×800px程度)
- **6枚とも配置・実装完了**。ユーザーが用意した画像は`.png.jfif`(実体はJPEG)というファイル名で来たため、`.jpg`にリネームしてコード側の参照も合わせた
- **通路プロンプトのハマりどころ**: 「奥に続く通路」で生成される問題が繰り返し発生した。「first-person view corridor stretching into darkness ahead」のような表現はもちろん、「side-on view, extending horizontally」と明示してもAIは奥行きのある通路を描きがちだった。最終的に**「Castlevania/Metroidvaniaのような2D横スクロールゲームの背景」と明言し、「flat, like a theater backdrop」「NOT a perspective corridor with a vanishing point」**まで書いて初めて安定して平面的な壁面構図になった。今後、横スクロール前提の背景を発注するときはこの言い回しを流用するとよい
- ダンジョン1で実機確認済み(通路イラスト+スクロール+松明の重ね描き、コンソールエラーなし)。ダンジョン2/3・ボス部屋は画像自体は目視確認済みだが、未クリアで入れないため実戦闘での動作は次回プレイ時に確認すること
- 「オブジェクト(分かれ道など)を背景に重ねて情報量を増やす」という発展案は、今回は見送り。背景が一段落したので次にやるならこれ
- **松明の重複を解消**: イラストに松明が描き込み済みのため、通路イラスト表示時はプロシージャルの松明(`drawTorches`)を重ねないよう修正(プロシージャルのフォールバック時のみ描画)
- **ボス階層突入時にポケモン風の斜めワイプ演出を追加**: 「通路→(黒い斜めのワイプ、左から)→ワーニング→ボス部屋」という場面転換の要望を受けて実装。`App.css`に`@keyframes bossWipe`(左から入って画面を覆い、保持後に右へ抜ける3点アニメーション、計3.8秒)を追加し、`DungeonPage.jsx`に`showBossWipe`状態を新設。既存のタイマー連鎖(ワーニング表示3秒)はそのまま維持しつつ、ワイプが画面を覆い切った直後(400ms後)に`bossFloor`を切り替えて背景をボス部屋に差し替えることで、切り替わりの瞬間が視界に入らないようにした。実機でDEBUGのマップ進行ボタンを連打してB5F到達→ワイプ→ボス部屋への切り替わりを確認、コンソールエラーなし
- BGM(mp3)をユーザーが試作。**音声ファイルの試聴・評価はできない旨を伝えた**(このツール環境に音声再生手段がないため)。実装(再生タイミング等)が必要になれば別途対応
- **バグ修正(ワイプ演出の場面転換が2回連続で起きて見える)**: 「初見じゃわからなくなりそう」との指摘で原因判明。旧実装は`bossFloor`をワイプが画面を覆っている間(400ms時点)に立てていたため、鍵なし自動退却の3秒カウントもその時点から始まってしまい、ワーニング終了(3400ms時点)とほぼ同時に退却が発生して場面転換が二重に見えていた。`bossFloor`を立てるタイミングをワイプが**抜け始める瞬間**(3400ms時点、実際にボス部屋が見え始める瞬間と一致)に変更し、退却カウントがそこから独立して3秒始まるようにして衝突を解消。あわせて、ボス部屋から離れる瞬間(退却・戦闘開始どちらも)にも短い0.9秒版の同じワイプを新設(`showBossExitWipe`、`bossFloor`がtrue→falseに変わったことを検知して自動発火)し、退場も唐突に見えないようにした
- **未解決: 背景が一瞬止まって見える件**: モンスター出現時のisPause起因ではなく「歩いている最中に脈絡なく定期的に起きる」とのことで、タイルの継ぎ目(合わせ鏡のラップ境界)を疑って調査した。①継ぎ目の計算式は数学的に2回検証したが不連続は見当たらず ②実機にconsole.warnで診断ログを仕込み、resize誤発火(canvas.width/heightのリセットでimageSmoothingEnabledが元に戻る現象、念のため修正済み)は初回マウント時の1回のみで周期的ではないと確認 ③フレーム時間(dt)の診断では40〜80ms程度の小さなコマ落ちを何度か観測したが、環境要因(自動テストツールがタブをバックグラウンド扱いした影響で2秒超の異常値も混入)の可能性が拭えず、確定的な原因はまだ特定できていない。次に気になったら、可能ならスクリーンショットや画面録画があると特定しやすい
- **UIリデザイン、木製の枠イラストを実装**: ユーザーが用意した`ui_frame_wood.png`(1024×1024、四隅に真鍮金具付きの木製枠)をCSSの`border-image`(9-slice)で`.rpg-panel`に組み込んだ。受け取ったファイルはまた`.png.jfif`(実体はJPEG、透過なし)という形式で、中央の「透明にしたい領域」がチェッカーボード柄として焼き込まれていたため、Pythonでチェッカーボード領域を検出(明るいグレー系統・中央から連結した領域をscipy.ndimage.labelで特定)して実際に透過(alpha=0)に変換してからPNG化した。境界の太さは実測88px(1024px中)、`border-image-slice:88`で四隅の飾りを保ったまま辺を引き伸ばせるようにした。「街」タブのステータス欄・ダンジョンボタンで実機確認済み、コンソールエラーなし。他の画面への展開はこれから
- **背景スクロールをCSSアニメーション方式に作り直し**: 「動かし方を変えて、画像を複数使ってでもスムーズにしたい」との要望を受け、canvasで毎フレームJS計算する方式(GCなどでメインスレッドが詰まると影響を受けやすい)から、**あらかじめ合わせ鏡タイル画像(元画像+反転コピーを横に並べた2倍幅の1枚)を生成しておき、CSSの`background-position`アニメーション(ブラウザのGPU合成任せ、メインスレッドの詰まりに強い)で流す方式**に変更。3ダンジョン分の通路イラストをPythonで事前合成(`bg_dungeon{1,2,3}_corridor_tile.jpg`)。ボス部屋もcanvasではなく単純な`<img>`(object-fit:cover)に変更、プロシージャル版のcanvas+rAFループはイラストが無い場合のフォールバックとしてのみ残した。実機でCSSアニメーションの発火(`animationPlayState`がisRunning等に応じてrunning/pausedに切り替わること)・ボス階層との切り替え・タイマー再開時の速度を確認、コンソールエラーなし
- **未解決のまま**: 「背景が一瞬止まる」件は結局、旧canvas方式のどこが原因か特定できなかった(継ぎ目の計算式・canvasリサイズ誤発火は除外できたが、40〜80msの小さなコマ落ちの原因は不明なまま)。今回のCSSアニメーション方式への切り替えでメインスレッド起因の詰まりの影響は原理的に受けにくくなったはずなので、改善しているか実際のプレイで確認してもらうこと
- **「街」タブをギルドの掲示板シーンに刷新**: 「ダンジョン選択はギルドの掲示板、貼り紙3枚。押すと『受注』印が押されてからダンジョンに到着。図鑑は本、クエストは掲示板の横、実績はトロフィー」という世界観の具体化要望を受けて実装。ユーザーが用意した5枚のイラスト(`guild_board.png`=貼り紙3枚付き掲示板/`guild_book.png`=図鑑用の本/`guild_trophy.png`=実績用トロフィー/`guild_quest_board.png`=クエスト用の巻物板/`stamp_juchu.png`=「受注」の朱印)を使用
  - 掲示板画像上の3枚の貼り紙の位置は、scipy.ndimage.labelで羊皮紙色の連結領域を検出して座標を実測(左紙x[117-357]y[158-470]等)し、%指定でテキスト・クリック領域を正確に重ねた
  - ダンジョンの貼り紙をタップすると、CSSアニメーション(`stampSlam`、0.55秒でスケール+回転しながら着地する演出)で「受注」の朱印がその場にスタンプされ、700ms後にダンジョンへ遷移するようにした(即移動ではなく「受注してから向かう」体験に変更)
  - 受け取った画像のうち2枚(`guild_board.png.png`/`guild_book.png.png`等)はまた二重拡張子だったが、今回は中身も正しくPNG+透過だったため単純リネームのみで対応(前回のようなJFIF変換は不要だった)
  - 実機で表示・スタンプ演出・ダンジョン遷移を確認済み、コンソールエラーなし。旧来の「ダンジョン一覧ボタン+図鑑/クエスト/実績ボタングリッド」はこの掲示板シーンに完全に置き換えた
- **壁+床のシーンに発展**: 「壁に掲示板・クエスト掲示板、床に本・トロフィーを配置したい」との要望で、`guild_wall.png`(木壁+松明+下端に石床が少し覗く構図)を背景に、その上に掲示板・クエスト掲示板(壁の右側)・本(床の左)・トロフィー(床の右)を%指定で重ねる構成にした。床イラストは「壁の下端に少し覗く石床で十分」とのことで別途は用意していない。掲示板の貼り紙の位置指定(%)は、掲示板を「壁の上に浮かぶ子要素」として`position:relative`+独自のaspect-ratioでラップしたことで、既存のロジックをそのまま変更せず使い回せた
- **バグ修正(Reactの警告)**: `DungeonCanvas.jsx`で`animation`(ショートハンド)と`animationPlayState`(個別プロパティ)を同じstyleオブジェクトに混在させていたため、"mix shorthand and non-shorthand properties"という警告が出ていた。`animationName`/`animationDuration`/`animationTimingFunction`/`animationIterationCount`に分解して解消
- **壁イラストを差し替え**: ユーザーがより横長(1408×768、松明なしのシンプルな木壁+石床)な`guild_wall`に更新。旧(779×768)と比べて大幅に横長になったため、掲示板・クエスト掲示板・本・トロフィーの位置指定(%)を計算し直し、掲示板+クエスト掲示板のグループが新しい壁の中央に来るよう再配置した(左右の余白がほぼ均等になるよう計算)。実機で確認済み、コンソールエラーなし
- **ギルド配置エディタを追加**: 「UIのサイズ配置を自分で決められるようにしたい」との要望を受け、モンスターのRigEditor/FloatRigEditorと同じ思想で`GuildLayoutEditor.jsx`(DEBUG専用)を新設。ギルドシーンの4要素(掲示板/クエスト板/本/トロフィー)の位置(%)を`src/data/guildLayout.js`の`GUILD_LAYOUT_DEFAULT`に切り出し、TownPage側は`useState`で保持してJSXから参照するよう変更。要素選択ボタン+フィールドごとの-1/-0.1/入力欄/+0.1/+1ステッパーで調整でき、背後のシーンにその場で反映される(TimerSettings.jsxのステッパーUIと同じ操作感)。「JSONをコピー」ボタンで`GUILD_LAYOUT_DEFAULT`定数としてそのまま貼り付けられる形式を出力する。実機で数値変更→背景のライブ反映を確認済み、コンソールエラーなし
- **ユーザーが配置エディタで調整した値を反映**: コピーされたJSONをそのまま`GUILD_LAYOUT_DEFAULT`に適用
- **鍵の救済メッセージを非表示に**: 「5階ごとのボス部屋には鍵が必要です…」のバナーが、一度鍵不足で詰まると`keyRescueClaimed`がtrueになるまで街画面に永続的に表示され続ける仕様だったため、「普段表示しないようにしたい」との要望で削除。救済鍵の購入手段自体は`ShopTab.jsx`の「特殊」タブに既に独立して表示されているため(`rescueKeyTmpl`のUI)、機能自体は失われていない
- **壁イラストを再度差し替え**: `guild_wall`が松明+開いた扉のあるバージョン(1408×768、今回は最初から透過PNGで問題なし)に更新。`guild_quest_board.png`も木枠なしの巻物単体(450×742)に簡略化されたため、コード側の参照パス・アスペクト比を合わせて修正
- **壁イラストがその後も2回更新**(1481×768→1408×768)。ユーザーが試行錯誤している間、都度コード側のaspectRatioを実ファイルに合わせて追従した。今後も差し替わったら同様に対応すること
- **図鑑/クエスト/実績を分かりやすく**: 「ホバー/タップで光る・浮き上がる演出」「アイコン下にラベル文字」「報酬がある分だけ数字バッジ」の3点を実装。`.guild-prop-btn`(App.css)でtranslateY+drop-shadowのホバー演出、各ボタン下に`rpg-heading`でラベル追加。バッジはクエスト(`QUEST_DEFS`のdaily+weeklyを対象、`get(q,player)>=target && !claimed`で判定)と実績(`ACHIEVEMENT_DEFS`、`get(player)>=target && !claimed`)それぞれの受け取り可能数を計算し、0件のときは非表示。実機で「実績バッジ=2」「クエストバッジ=0(非表示)」を確認、ラベルの表示位置がクエスト巻物の縦横比のせいでトロフィーと重なっていた問題はquestBoard.widthを16→11に縮小して解消
- **「街」タブを「ギルド」に改名**: タブラベルと画面見出しの2箇所を変更。ギルド掲示板シーンの世界観に合わせた
- **キャラ画面の重複表示を削除、配置エディタを追加**: 「武器の上のLvいらないかも」との指摘で、装備欄上部の「Lv{lv}」+経験値バーを削除(全画面共通ヘッダーに既に同じ情報が表示されているため冗長だった、未使用になった`expToLevel`等のimportも削除)。あわせて「ここもステータスとか装備欄とか位置・大きさを自分で調整したい」との要望で、GuildLayoutEditorと同じ思想の`CharacterLayoutEditor.jsx`(DEBUG専用)を新設。部屋の高さ・装備枠サイズ・特殊枠サイズ・ステータス欄の幅の4項目を`src/data/characterLayout.js`の`CHARACTER_LAYOUT_DEFAULT`に切り出し、-5/-1/入力欄/+1/+5のステッパーでその場調整→JSONコピーで恒久反映できる。実機で装備枠サイズの変更がライブ反映されることを確認済み、コンソールエラーなし
- **キャラ配置エディタに位置調整項目を追加、暗幕を撤去**: 「配置も変えたい、エディタを開くと他が暗くなって操作しづらい」との指摘を受けて改善。①`roomPanY`(部屋イラストの縦方向の見せ方)・`contentPaddingTop`(全体を部屋の上端からどれだけ下げるか)・`panelGap`(装備パネルとステータスパネルの間隔)の3項目を追加し、サイズだけでなく配置も調整できるようにした ②エディタを画面全体を覆う暗い半透明モーダルから、右下に浮かぶ小さいパネル(暗幕なし)に変更し、背後のシーンを見ながら調整できるようにした。実機で位置調整のライブ反映を確認済み、コンソールエラーなし
- **キャラ配置エディタに個別配置モードを追加**: 「装備欄一つ一つの配置、ステータスもこれをしっかり決めたい」との要望を受け、装備枠4つ・特殊枠3つ・ステータス6つ、計13項目を個別に位置調整できるようにした。実装は各項目のラッパーに`position:"relative"`+`left`/`top`(通常のグリッド/フレックス位置からのズレ、px、デフォルト0,0)を追加するだけで、既存のグリッドレイアウト自体は変更していない(0,0のままなら見た目は変わらない)。位置データは`characterLayout.js`の`equipItemPos`/`specialItemPos`/`statItemPos`に追加。エディタには「全体」(既存7項目)/「個別配置」(13項目)のモード切り替えタブを新設。実機でMAGのステータス行を個別に動かし、他の行に影響せず単独で移動することを確認済み、コンソールエラーなし
- **ステータス欄にもイラストの枠を追加**: 「ステータスは防具と同じように枠を用意する？」との提案を受け、装備枠(`equip_slot_frame.png`)と統一感のある横長プレート枠`stat_plate_frame.png`(1183×549、装飾的な唐草模様が上下にはみ出る意匠)を用意してもらい、ステータス各行(ATK/MAG/DEF/MDEF/EVA/CRIT)の背景として組み込んだ(旧来のrgba単色背景を置き換え)。実機で確認済み、コンソールエラーなし
- **バグ修正(ステータス欄の枠が潰れて文字が枠にかぶる)**: 「もっと大きく、文字が枠にかぶってる」との指摘。原因は`stat_plate_frame.png`(自然比率1183:549)を`width:100%,height:100%`で当てはめていたのに、コンテナ自体の高さがテキスト+パディングだけで決まっていたため、frame画像が本来より大幅に縦潰れして表示され、枠の縁取り部分が極端に薄くなり文字と衝突していた。コンテナに`aspectRatio:"1183/549"`を指定して画像本来の比率で表示されるようにし、中身のpaddingも固定pxから枠の凹み部分に収まるinset(26% 13%)に変更。結果的にプレート全体が縦に大きくなり、視認性も改善した。実機で確認済み、コンソールエラーなし
- **装備スロットにイラストの枠を追加**: ユーザーが用意した`equip_slot_frame.png`(422×416、真鍮/鉄の縁取り+奥まった暗いソケット、透過)を装備スロット・特殊スロットに適用。レアリティ色分けは枠自体には焼き込まず、**枠の背後にぼかしたグロー(blur+opacity)として表現**する方式にした(枠はどのレアリティでも同じニュートラルな金属色のまま使い回せる)。ソケットの内側余白は目視で約19%と見積もり`inset:"19%"`でアイコンを配置。実機でアイテム装備→グローが色付きで光ることを確認済み、コンソールエラーなし
- **キャラ画面を「1枚背景+半透明レイヤー」方式に作り直し**: ギルドと同じく「背景1枚(ごちゃごちゃしていない)+その上にUIを重ねる」方針で要望を受け、ユーザーが用意した`character_room.jpg`(1408×768、武具スタンド+マネキン+夜具のある簡素な部屋)を装備サマリー欄の背景に採用。旧`character-banner.png`ヘッダー(90px)は廃止し、装備スロット・特殊スロット・HP・ステータス欄をすべて半透明の暗色カード(`rgba(15,10,5,0.72)`+金色の細い縁取り)として部屋の上に重ねる形にした。ドラッグ&ドロップの装備・アイテム選択の動作は変更なし、実機で確認済み、コンソールエラーなし。装備関連(ドラッグ&ドロップ枠・レアリティ色分け)は既存の見た目を維持
- **キャラ画面(CharacterPage.jsx)にも暖色テーマを展開開始**: 「他のタブも同じように改良していこう」との要望を受けた第一弾。ヘッダーの「CHARACTER」見出し・装備/スキルタブの選択色を紫(#a78bfa)から金(#e0b555)に統一、装備サマリー欄の背景を暖色グラデーションに、ステータス数値欄(ATK/MAG等)の背景も焦げ茶色+縁取りに変更。ドラッグ&ドロップの装備システムやレアリティ色分け(アイテム枠・素材)など機能的な色分けはそのまま残し、外枠・見出しなど「AIっぽさ」が出やすい部分から着手。実機で装備選択・アイテム詳細表示が壊れていないことを確認済み、コンソールエラーなし。ショップ・鍛冶屋タブは未着手、次回以降
- **ダンジョンの貼り紙に蝋封印イラストを追加**: 各ダンジョンのテーマ(1=石造り→狼、2=古城→塔、3=溶岩→竜)に合わせた蝋封印(`dungeon_seal_1/2/3.png`)を用意し、解放済みの貼り紙の🚪/🏆絵文字と差し替えた(クリア済みは封印の右下に🏆を小さく重ねる)。ロック中の貼り紙は従来通り🔒のまま。「ダンジョン1などの文字が見づらい」との指摘も受け、見出しをfontWeight:900+濃い焦げ茶色(#241608)+白のtextShadowでコントラストを強化、ステータス行もfontWeight:700に太くした

**公開準備(Vercelデプロイ済み)**

- [study-rpg-wbq1.vercel.app](https://study-rpg-wbq1.vercel.app/) で公開中。GitHub `main`へのpushで自動再デプロイされる
- タイトルを「スタディダンジョン」に統一(index.htmlのtitle/meta description/OGP、vite.config.jsのPWA manifest)。街画面上部の小さな装飾ラベルだけ「STUDY DUNGEON」の英語表記が未統一のまま残っている(実害なし、気になれば直す)
- DEBUGツール(モンスター召喚・リグエディタ等)は`import.meta.env.DEV`ガードで本番ビルドから完全に除外されることを確認済み(本番で目視・ビルド出力の両方で確認)
- Firebase設定は環境変数ではなく`src/firebase/config.js`にハードコードされているため、Vercel側で追加の環境変数設定は不要。本番での匿名ログイン・Firestore永続化も動作確認済み(IndexedDBに正しいUID・プロジェクトIDでキャッシュが作成されることを確認)
- **意見箱機能を追加、ただしFirestoreルール未設定で動作しない**: `src/firebase/feedback.js`の`submitFeedback()`が`feedback`コレクションへの書き込みを試みるが、Firebaseコンソール側のセキュリティルールに`feedback`コレクション用の許可がまだ無く、「Missing or insufficient permissions」エラーになる。**次にやること**: ユーザー自身がFirebaseコンソールのFirestoreルールに以下を追記する必要がある(このルール変更はコード側からはできない):
  ```
  match /feedback/{feedbackId} {
    allow create: if request.auth != null;
    allow read, update, delete: if false;
  }
  ```
  設定後、SettingsPage.jsxの意見箱フォームから送信テストして確認すること。**本番で送信テスト済み、正常動作を確認**
- **イベントが4回起きず2回で終わる不具合を修正**: 「残り5分なのに1回しか起きてない、このセットは2回で終わった」との報告。原因は旧スケジューリングが「6分おきに85%抽選」という確率任せの仕組みだったため、25分セッションだと抽選機会が実質4回(4分/10分/16分/22分)しかなく、外れが重なると4回に届かないまま終わってしまう欠陥があった。**作業時間をBASE_MAX_EVENTS(4)等分し、各区間内のランダムな時点で抽選なしに確定発生させる方式に再設計**(`workMin`に応じて動的に区間を計算するため、作業時間を変更しても常に4回保証される)。戦闘中/イベント表示中で発生できないタイミングだった場合は1秒おきに再試行し、取りこぼさないようにした
  - 合わせて「2回目のイベントが45秒のはずが20秒で終わった」との報告もあったが、これはコード上は該当箇所が見当たらず、**PWAのService Workerが新ビルドを裏で取得していても、開いたままのタブは完全リロードするまで旧バージョンのJSで動き続ける**ことが原因の可能性が高い(vite-plugin-pwaのautoUpdate方式の既知の挙動)。今後動作確認する際は、事前にタブを一度完全に閉じて開き直す(またはハードリロード)ことを徹底する

**アイテムの色替え(リカラー)システム**(モンスターイラスト化ロールアウトは全30種完了、下の「完了したこと」参照)

- 既存アイテムの画像をCSS `filter`(インラインstyle)で色替えし、新規イラストなしで色違いアイテムを量産する仕組みを確立。武器7点(黒曜の大剣/紅蓮の杖/猛毒の杖/聖光の杖 他)・防具4点(聖なるローブ/業火のローブ 他)・アクセ2点(紅玉の指輪/月光の護符)、計13点を追加済み。innateは全て既存のものを再利用(新規ステータス効果は未実装)
- **色替えパラメータの勘所(画像によって最適値が大きく変わる)**:
  - 剣などグレー基調・低彩度の金属質画像 → `sepia(1)`を先にかけてから`hue-rotate`(モノクロ画像の定番テクニック)
  - 杖の水晶球など極端に彩度が低い部分 → `saturate`を2.4〜3.5倍程度まで強くしないと色が乗らない
  - ローブなど元々彩度の高い生地 → `sepia`は使わず、素材本来の色相から目標色相までの差分を直接計算した`hue-rotate`のみで対応(sepia先掛けだと変な中間色になる)
  - 毎回オフラインのPythonスクリプト(scratchpad)で試し打ちしてから実機のブラウザで最終フィルタ文字列を直接テストする、の2段階検証が確実
- アイテム画像を表示している箇所(CharacterPage.jsx/ForgeTab.jsx/ShopTab.jsx/DungeonPage.jsx、計9箇所)全てに`filter:item.tint||"none"`対応済み(既存のgrayscaleフィルタとは`${tint} ${grayscale}`の形で共存)
- **次にやること**: 消耗品・特殊アイテムへの適用は未検討。新属性(氷・聖・闇など新しいinnate追加)をやる場合は`systems/status.js`への実装が必要になる(現状は既存innateの再利用のみ)。「妖怪ウォッチ的にモンスターを3倍にする」という発展案は別スコープとして保留中(ユーザー確認済み)

**未解決・要フォローアップ**

- **竜(火竜/若竜/黒竜)の4足歩行の対角トロットの揺れ方**: 「違和感が強い」との指摘を受けswingAmpを拡大(前脚0.20→0.34、後脚0.17→0.28)して対応したが、**ユーザーからは「何も変わってない」との反応があり、修正できたか未確認のまま次のタスクに移った**。次回このモンスターを見る機会があれば実機で確認すること。`FloatRigEditor.jsx`に位相(phase)・揺れ幅(swingAmp)・walkSwingの数値編集機能を追加済みなので、ユーザー自身がリアルタイムで追い込むこともできる

## 完了したこと(時系列)

### モンスターイラスト化ロールアウト(全30種、完了)
- 粘体5/獣5/植物4/不死4/ゴブリン4/悪魔4/竜4の全30種を実イラスト化。パーツ分けの要否は種族ごとに旧ベクター描画関数(`DRAW_FUNCS`)を必ず確認してから判断する方針を徹底し、「動きの複雑さは見た目の複雑さと比例しない」(植物・不死は想定よりずっとシンプルだった)という教訓を得た
- 描画パターンを7種類確立(`multipartMonsters.js`/`MonsterSprite.jsx`): `MULTIPART_MONSTERS`(4足歩行・獣系)/`SIMPLE_IMAGE_MONSTERS`(1枚絵・粘体系)/`GROUP_IMAGE_MONSTERS`(複数体の群れ)/`SWAY_IMAGE_MONSTERS`(植物の根元スイング)/`FLOAT_IMAGE_MONSTERS`(1枚絵の浮遊のみ)/`FLOAT_RIG_MONSTERS`(浮遊+付属パーツ)/`GROUND_RIG_MONSTERS`(地上アンカー+付属パーツ、歩行中のみ大きく揺れる`walkSwing`対応)
- 座標調整ツールを`RigEditor.jsx`(獣系専用・3パーツ固定)→`FloatRigEditor.jsx`(`config.parts`可変長配列に対応した汎用版)の順に開発し、後者は大幅リニューアルを経て**パーツ選択ボタン・数値入力欄(anchor/pivot/scale/swingAmp/phase)・反転(mirror)/奥行き(behind)/walkSwingのトグルボタン・全体ズーム・自動フィット**を備えるに至った。位置・向き・揺れ方までほぼ全てユーザー自身が実機を見ながらリアルタイムで調整できる状態
- **今後も有効な技術的教訓**:
  - **mirror(左右反転)は回転の向きも反転させる**ため、揺れ角の符号を`p.mirror ? -sway : sway`で反転する必要がある(`drawFloatRig`/`drawGroundRig`で対応済み)
  - 画像は「幅」ではなく**「長い方の辺」を基準にスケール**する(`fitScale(size,img,k)`ヘルパー)。体画像が全身像でなく胴体だけの場合は`config.bodyScaleK`で倍率を上書きする
  - パーツの`scale`は基本1.0のままでよい(描画後のサイズ = content_px × scale で決まり、**キャンバスの余白(パディング)量はスケールに一切影響しない**。余白が影響するのは付け根位置=anchor/pivotの計算だけ)
  - **左右判定(mirror要否)は座標やロジックからの推測より、実際に画像を並べて見た目を比較する方が確実**(推測ベースの変更で何度か手戻りが発生した)
  - モンスターの色替え(`ctx.filter`)技術もここで確立(若竜・黒竜は火竜のイラストを`hue-rotate/saturate/brightness`で色替えしただけで追加、新規イラストなし)。この技術がのちにアイテム色替えにも展開された
- 竜の4足歩行の対角トロットの揺れ方は最終的にswingAmp拡大で対応したが動作確認は未完了(「進行中」セクション参照)

### 獣(四足)tribe 5体が完了

### 獣(四足)tribe 5体が完了
- オオカミ・ダイアウルフ・重装イノシシ・血狼の首領を`MULTIPART_MONSTERS`に追加
- 最初はフェンリルの最終調整済みanchor/pivot値をそのまま初期値に流用。オオカミは実機で自然に見えたが、**ダイアウルフと血狼の首領は脚が浮く・尻尾が変な方向を向くという明確な不具合が発生**(ユーザー指摘で発覚)
- 原因調査①(anchor/pivotのズレ): Pythonで各パーツ画像(前脚/後脚/尻尾)の透過ピクセルを解析し、付け根側の重心位置を計測。フェンリルの実測値と比較したところ、ダイアウルフの前脚は軸がX0.50のつもりが実際は0.70など、いずれも大きくズレていたことが判明。実測値で一旦修正 → 最終的にはユーザーがRigEditorで微調整した値を反映
- 原因調査②(向きが逆、ユーザー指摘で発覚): ダイアウルフ・血狼の首領の体画像を再確認したところ、**フェンリル/オオカミとは違い、元イラストの顔が最初から右向きだった**。他の個体に合わせて`faceRight:false`(左右反転)を設定していたため、右向きの絵をさらに反転させて左向き(プレイヤーに背を向ける向き)になってしまっていた。両者を`faceRight:true`に修正して解決
- 修正後は戦闘開始直後の全力疾走ポーズ(walk-in、フルスイング)でも脚・尻尾の浮き・向きの問題が解消されたことを実機確認済み
- イノシシだけ体型が違うので初期値を少し変えたが、それでも実用レベル
- これで獣系tribeの5体(オオカミ/ダイアウルフ/フェンリル/重装イノシシ/血狼の首領)が全て実イラスト化完了、コンソールエラーなし

### 主人公スプライトのAI歩行アニメ化(PixelLab採用)
- 背景: モンスターのパーツ生成→RigEditor手動調整の工程が1体ごとに時間がかかるとの指摘があり、代替手段を検索。**PixelLab.ai**・**Sprite AI**(sprite-ai.art)を調査(詳細は[monster_illustration_prompts](https://claude.ai/code/artifact/b97ca25f-08cd-4546-b0bb-a496ab271d14))。どちらもキャラ1枚の静止画から歩行/攻撃アニメを自動生成できるサービス
- 主人公(PlayerSprite)で検証: PixelLab.aiの「Animation with Text」で8フレームの歩行スプライトシートを生成(`public/assets/images/player_walk_1.png`〜`_8.png`、各64×64)
- `PlayerSprite.jsx`に`drawPlayerSprite`を追加。`animFrame`に応じてフレームを切り替えるだけのシンプルな実装(モンスター側の`drawPart`のような回転・アンカー計算は不要)。画像未読み込み時は既存のベクター描画(`drawPlayer`)にフォールバック
- 敗北時のフェードアウト退場、被弾時の赤フラッシュ、HPバー表示は元のロジックを維持したまま画像版に対応
- フレーム切り替え間隔を微調整(2.5→6、ゆっくりに)
- 実機で歩行アニメーションの切り替わりを確認済み、コンソールエラーなし

### 植物tribe 4体が完了
- 苔スライム・世界樹の苗木・食人植物・古の魔樹を実装。既存のベクター版`drawPlant`が脚を持たず幹〜葉〜花〜目を一体で根元支点に揺らすだけだったため、パーツ分けせず**新設の`SWAY_IMAGE_MONSTERS`(`multipartMonsters.js`)+`drawSwayCreature`(`MonsterSprite.jsx`)**で対応。根元付近の1点(pivot)を軸に画像全体を`ctx.rotate`で揺らすだけの実装
- 古の魔樹だけ背景が不透明(白)だったため、Pythonで四隅からの塗りつぶし(flood fill)で透過処理してから使用
- 実機で4体とも接地・揺れを確認済み、コンソールエラーなし

### 不死tribe 4体が完了
- スケルトン・デスナイト・亡霊騎士・亡国の女王を実装。当初は「1枚絵のみで十分」と設計したが、実際に届いたイラストはパーツ分けされていた(スケルトン=体+左腕+右腕+足、亡霊騎士=体+剣、亡国の女王=胴体+下半身+腕、デスナイトのみ単独1枚絵)ため、届いた素材に合わせて2つの新しい仕組みを追加:
  - **`FLOAT_IMAGE_MONSTERS`**(デスナイトのみ使用): 1枚絵をただ上下に浮遊させるだけ(`drawFloatCreature`)。潰れ・伸び・回転なし
  - **`FLOAT_RIG_MONSTERS`**(スケルトン・亡霊騎士・亡国の女王): 体は浮遊するだけで、腕・脚・剣などの付属パーツをアンカー+ピボットで取り付けて個別に揺らす(`drawFloatRig`)。獣系の`drawPart`をそのまま再利用し、歩行(walking)の概念がない浮遊系の揺れに差し替えた。`drawPart`に`mirror`引数を追加し、スケルトンの足1枚を左右反転して2本の脚に見せている
  - **注意**: RigEditorはまだ`FLOAT_RIG_MONSTERS`形式に対応していないため、anchor/pivotは全て画像を目視して配置したbest-effort値(実機で大きな破綻がないことは確認したが、精密な調整はできていない)。ユーザーから見た目のフィードバックがあれば都度座標を修正する運用
- 実機で4体とも大きな崩れがないことを確認済み、コンソールエラーなし
- モンスター側は結局既存パーツ方式が「値の使い回し」で十分速くなったため、PixelLab方式は今のところ主人公のみで採用

### 粘体(スライム系)5体の実装完了
- `SIMPLE_IMAGE_MONSTERS`に`giant_slime`(巨大スライム)・`acid_slime`(酸スライム)・`abyss_slime`(深淵スライム)を追加。スライムと同じ`drawImageCreature`の定数がそのまま使えたため調整不要
- `スライム軍団`だけは大/中/小3枚の別々の画像が届いたため、新しく**`GROUP_IMAGE_MONSTERS`**という仕組みを追加(`multipartMonsters.js`・`MonsterSprite.jsx`の`drawImageGroup`)。3体を異なるX位置(`dx`)・縮尺(`scale`)・バウンドの位相(`phase`)で同時描画し、群れらしく見えるようにした。最初は重なりすぎていたので`dx`/`scale`を実機で見ながら調整済み
- これで粘体系tribeの5体(スライム/スライム軍団/巨大スライム/酸スライム/深淵スライム)がすべて実イラスト化完了

### モンスターイラスト化基盤の確立
- `src/data/multipartMonsters.js` に `MULTIPART_MONSTERS`(パーツ分け用)と`SIMPLE_IMAGE_MONSTERS`(1枚絵用)を分離(元は`MonsterSprite.jsx`内にあったが、react-refresh/only-export-componentsのlintに引っかかるため別ファイル化)
- **パーツ分け方式**(四足・二足など動きが複雑な体型向け): `MonsterSprite.jsx`の`drawPart`/`drawMultipartCreature`で体・前脚・後脚・尻尾などを個別画像として描画し、脚の付け根を軸に回転させて歩行アニメーションを作る
  - フェンリルで確立: 画像は`public/assets/images/フェンリル_胴体.png`等(AI生成・透過PNG)。前脚・後脚それぞれ1枚しかないので同じ画像を奥/手前2回描画して4本脚に見せる
  - 位置調整が言葉のやり取りだけでは限界があったため、**`src/components/RigEditor.jsx`という専用DEBUGツールを作成**: `MULTIPART_MONSTERS`の設定オブジェクトを参照渡しで直接ミュータブルに編集するReactコンポーネント。実イラストを使ってドラッグで付け根位置(anchor)とパーツ自身の回転軸(pivot)を調整でき、書き換えが即座にバトル画面の実際のモンスターにも反映される(ライブプレビュー)。「中心点を移動」ボタンで表示位置全体もずらせる。「DEBUG: リグエディタ」ドロップダウンから`MULTIPART_MONSTERS`に登録済みの任意のモンスターを選んで開ける
  - フェンリルの最終座標(ユーザー確認済み): `frontLegAnchor:{0.48,0.64} backLegAnchor:{0.84,0.67} tailAnchor:{0.88,0.63} frontLegPivot:{0.50,0.02} backLegPivot:{0.34,0.05} tailPivot:{0.07,0.38}`
- **1枚絵方式**(粘体など動きが単純な体型向け): `MonsterSprite.jsx`の`drawImageCreature`で、画像1枚に潰れ・伸び・バウンドのcanvas変形だけを適用(旧`drawSlime`の数式をそのまま流用)。RigEditor不要、実装コストほぼゼロ
  - スライムで確立: `public/assets/images/スライム.png`。接地位置(groundYからのオフセット)を実機で見ながら微調整済み
- **新モンスターを追加する手順**(ユーザー承認済みのワークフロー):
  1. ユーザーがAIイラストでパーツ画像(体型が複雑な場合)または1枚絵を用意し、`public/assets/images/`に配置
  2. `multipartMonsters.js`の`MULTIPART_MONSTERS`(パーツ分け)または`SIMPLE_IMAGE_MONSTERS`(1枚絵)にエントリを追加。パーツ分けの場合anchor/pivotは仮の値(0.5付近)でよい
  3. パーツ分けの場合: DungeonPageのDEBUG画面「DEBUG: リグエディタ」で該当モンスターを選び、ドラッグで調整 →「JSONをコピー」→ チャットで共有してもらい反映
  4. 1枚絵の場合: `debugSpawnMonster`で実機確認しつつ`drawImageCreature`のオフセット定数を微調整するだけ
- 四足系(白銀の狼王など)はfenrirと同じパーツ構成でそのまま使い回せる見込み。二足系(骸骨の将)や翼のある竜系は`PART_KEYS`(`RigEditor.jsx`)とdrawMultipartCreatureの構成自体を拡張する必要がある(未着手)
- テスト用: DungeonPageに「DEBUG: モンスター召喚」ドロップダウンと「DEBUG: リグエディタ」ドロップダウンあり(DEV環境のみ表示)

### 戦闘バランス調整(25分セッション向け)
- `src/systems/monsters.js`: `MONSTER_HP_SCALE`(1.4)・`MONSTER_ATK_SCALE`(1.15)・`BOSS_HP_SCALE`(1.4)・`BOSS_ATK_SCALE`(1.15)・`REWARD_SCALE`(1.25)を追加。ATKはレアリティ倍率と重なると事故るため控えめにした
- 星ランク(敵の強さ)出現確率を階層に応じて線形補間(`EARLY_STAR_PROBS`→`LATE_STAR_PROBS`)。浅い階は星1中心、深い階ほど星3/4が増える
- `src/components/BattleEffect.jsx`: 1ターンのアニメーション時間を1500ms→2200ms

### イベント頻度・演出
- イベント回数は4回固定を維持(連敗による無報酬連鎖のリスクを増やさないため)。最初の抽選だけ6分後→3分後に前倒し
- NPCイベントの会話文にバリエーション追加、妖精がゲームのヒントを喋るように
- HP回復イベントを瞬間全回復ではなく2.4秒かけて満ちるアニメーションに変更
- 画面上部に大きめのイベントポップアップを追加(見落とし防止)
- **「開始10分近く何も起きない」との指摘で修正**: 最初のイベントは3分後に抽選(当選率85%)という仕様だったため、15%の確率でハズレると次の抽選が6分後になり、合計9分間無風になりうる欠陥があった。最初のイベントだけ抽選を撤廃し、4分後に確定発生するように変更(`tick(delay, guaranteed)`にguaranteedフラグを追加)。2回目以降は従来通り6分おき・当選率85%の抽選を維持
- **妖精/罠/NPC会話イベントの表示時間を7秒前後→45秒に延長**: 「もう少しじっくり読ませたい、30秒〜1分くらいの尺でもいい」との要望。`EVENT_POPUP_DURATION_LONG`定数を新設し、trap・npc/fairy/spirit系イベントに適用。宝箱(7秒)とHP回復(8秒、専用の充填アニメあり)は据え置き

### 合成ルール変更
- 装備合成: 同じ種類+レアリティ → 同じ**サブタイプ**(剣は剣、弓は弓)も一致必須に変更(激甘だったバランス調整)
- スキル書合成: 完全一致(同じID)必須 → 同じ**系統(tree)+アクティブ/パッシブ区分**なら別IDでも可に緩和(激渋だったバランス調整)

### イラスト実装
- アイテム47種(武器21・防具7・アクセサリー6・消耗品7・特殊6)に実イラストを追加。`items.js`に`image`フィールド、各UIコンポーネントで`icon`(絵文字)からのフォールバック対応
- バナー4枚(実績/図鑑/クエスト/キャラ画面)
- 宝箱4種(木箱/銀箱/金箱/虹箱)。開閉2枚をAI生成すると同一の箱に見えない事故が起きやすいため、閉じた状態1枚のみ+既存のパーティクル演出で「開いた感」を表現する方針
- イラスト生成プロンプト集をArtifactとして公開・維持している(v3まで改訂: 白背景・無発光・斜め45度統一・トリミング指定)

### コード健全性ロードマップ(6項目、全完了)
1. 宝箱プレビューへの画像反映、lintエラー2件(未使用変数)解消
2. (1に統合)
3. キャラ画面のモバイル対応(768px未満で縦積みレイアウト、タップ領域44px化)
4. 戦闘アニメーションのuseEffectが毎秒(タイマー更新のたび)再起動していたバグ修正。コールバックpropsをrefに格納する形に変更(BattleEffect/MonsterSprite/EventSprite)
5. vitest導入。timer.js/battle.js/monsters.jsのバランスロジックに23件のテスト追加(`npm test`)
6. Firestoreのオフライン永続化を有効化(`persistentLocalCache`)。HMR等での二重初期化エラーはgetFirestore()フォールバックで対処

## 既知の注意点

- 開発サーバーの特定タブ("seed"タブ)で、過去のHMR異常時のFirestoreエラーがコンソールに残り続けることがある。**これは実害のないスタックログ表示の問題で、新しいタブで開けばエラーは出ない**(実機で複数回確認済み)。本物のエラーと誤認しないよう注意
- `src/data/items.js`の`SYNTHESIS_COST`に残る`mat`/`matCount`フィールドは未使用の死んだデータ(実害なし、消してもよい)
